using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyDeathCollision : MonoBehaviour
{
    /*

    [SerializeField] private EnemyController enemyControllerScript;

    [SerializeField] private BoxCollider2D myCollider;



    private Collision2D otherCollision;

    private void OnCollisionEnter2D(Collision2D collision)
    {
        if(collision.gameObject.tag == "Player")
        {
            //Debug.Log("Ignore collision with player!");
            otherCollision = collision;
        }
    }

    public void IgnoreCollisionWithPlayer()
    {
        Physics2D.IgnoreCollision(myCollider, otherCollision.otherCollider, true);
    }
    */
}
