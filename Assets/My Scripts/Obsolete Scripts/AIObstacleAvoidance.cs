using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AIObstacleAvoidance : MonoBehaviour
{
    /*
    [Header("Required Scripts")]
    [SerializeField] private EnemyController enemyControllerScript;
    [SerializeField] private EnemyMovement enemyMoveScript;

    [SerializeField] private float distanceWalkAwayMultiplier = 4f; // a multiplier to the distance this enemy will move away from the obstacle

    private void OnTriggerEnter2D(Collider2D collision)
    {
        //OnTriggerStay2D continues to run even when the script component is disabled
        if (!this.enabled)
            return;

        //if this avoidance touches an enemy or obstacle
        if(collision.gameObject.layer == LayerMask.NameToLayer("Enemy") || collision.gameObject.layer == LayerMask.NameToLayer("Obstacle"))
        {
            //Vector2 newPos = Vector2.MoveTowards(transform.position, targetPosition, 2f);

            //enemyControllerScript.ChangeEnemyState(0f, EnemyController.EnemyState.Avoiding);

            //enemyMoveScript.MoveTowards(newPos);
            

        }



    }
    */
}
